plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.1'
    id 'io.spring.dependency-management' version '1.1.5'
}
allprojects {
    repositories {
        mavenCentral()
    }
}

bootJar {
    enabled = false
}//부모폴더 bootjar 생성 안함

jar {
    enabled = false
}//부모폴더 jar 생성 안함